apiVersion: v1
kind: Service
metadata:
  labels:
    app: container-basics-training
  name: container-basics-training
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: container-basics-training
  sessionAffinity: None
  type: ClusterIP
